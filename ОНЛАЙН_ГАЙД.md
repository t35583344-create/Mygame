# 🌍 Как играть ОНЛАЙН с другом из другой страны

Выберите **ОДИН** из способов ниже:

---

## Способ 1: ⭐ САМЫЙ ПРОСТОЙ - Используем Replit (Бесплатно)

### Шаг 1: Загружаем на Replit
1. Перейдите https://replit.com
2. Создайте аккаунт (можно через GitHub)
3. Нажмите **"Create Repl"**
4. Выберите **Node.js**
5. Скопируйте содержимое внизу и вставьте файлы

### Шаг 2: Создаёте файлы на Replit
Создайте 3 файла:

**Файл 1: `server.js`**
```javascript
const WebSocket = require('ws');
const http = require('http');
const express = require('express');
const path = require('path');

const app = express();
const PORT = process.env.PORT || 3000;

app.use(express.static(path.dirname(__filename)));

const server = http.createServer(app);
const wss = new WebSocket.Server({ server });

let players = {};
let grid = Array(20).fill(null).map(() => Array(30).fill(null));
let nextPlayerId = 1;

const playerColors = ['#ff69b4', '#FFD700', '#00CED1', '#00FF00', '#FF6347', '#FF4500', '#9370DB', '#20B2AA'];

wss.on('connection', (ws) => {
    let playerId = null;
    let playerName = null;

    ws.on('message', (data) => {
        try {
            const message = JSON.parse(data);

            switch (message.type) {
                case 'join':
                    playerId = nextPlayerId++;
                    playerName = message.name;
                    const playerColor = playerColors[(playerId - 1) % playerColors.length];

                    players[playerId] = {
                        id: playerId,
                        name: playerName,
                        x: 150 + Math.random() * 300,
                        y: 50,
                        color: playerColor,
                        ws: ws
                    };

                    ws.send(JSON.stringify({
                        type: 'yourId',
                        id: playerId
                    }));

                    ws.send(JSON.stringify({
                        type: 'gridUpdate',
                        grid: grid
                    }));

                    ws.send(JSON.stringify({
                        type: 'playersList',
                        players: Object.values(players)
                    }));

                    broadcastAll({
                        type: 'playerJoined',
                        playerId: playerId,
                        name: playerName,
                        x: players[playerId].x,
                        y: players[playerId].y,
                        color: playerColor
                    });
                    break;

                case 'movePlayer':
                    if (players[playerId]) {
                        players[playerId].x = message.x;
                        players[playerId].y = message.y;
                        broadcastAll({
                            type: 'playerMove',
                            playerId: playerId,
                            x: message.x,
                            y: message.y
                        });
                    }
                    break;

                case 'placeBlock':
                    if (message.y < grid.length && message.x < grid[0].length) {
                        grid[message.y][message.x] = message.color;
                        broadcastAll({
                            type: 'blockPlaced',
                            x: message.x,
                            y: message.y,
                            color: message.color
                        });
                    }
                    break;

                case 'deleteBlock':
                    if (message.y < grid.length && message.x < grid[0].length) {
                        grid[message.y][message.x] = null;
                        broadcastAll({
                            type: 'blockDeleted',
                            x: message.x,
                            y: message.y
                        });
                    }
                    break;
            }
        } catch (e) {
            console.error('Error:', e);
        }
    });

    ws.on('close', () => {
        if (playerId && players[playerId]) {
            delete players[playerId];
            broadcastAll({
                type: 'playerLeft',
                playerId: playerId
            });
        }
    });
});

function broadcastAll(message) {
    wss.clients.forEach(client => {
        if (client.readyState === WebSocket.OPEN) {
            client.send(JSON.stringify(message));
        }
    });
}

server.listen(PORT, () => {
    console.log('🎮 Server running on port ' + PORT);
});
```

**Файл 2: `package.json`**
```json
{
  "name": "block-builder-multiplayer",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.13.0"
  }
}
```

**Файл 3: Скопируйте содержимое `index.html` в Replit**

### Шаг 3: Запустите
1. Нажмите **"Run"** вверху
2. Вы увидите URL (типа `https://username-block-builder.replit.dev`)
3. **КОПИРУЙТЕ это URL!**

### Шаг 4: Поделитесь с другом
- Отправьте другу этот URL
- Друг откроет его в браузере
- Оба введите свои имена
- Вместо "localhost:8081" вставьте URL без `https://`
- **Нажмите "Join Game"**

---

## Способ 2: Используем Railway (Бесплатно, лучше для вас)

1. Перейдите https://railway.app
2. Нажмите **"Deploy from GitHub"** или создайте новый проект
3. Загрузите свои файлы (или свяжите GitHub репозиторий)
4. Railway автоматически запустит сервер
5. Получите публичный URL
6. Поделитесь URL с другом

---

## Способ 3: Используем Heroku (требует карты)

1. Создайте аккаунт на https://www.heroku.com
2. Создайте новое приложение
3. Загрузите файлы через GitHub или Heroku Git
4. Сервер автоматически запустится
5. Получите URL вашего приложения

---

## ✅ Что вводить в игре (для онлайн):

Когда открываете `index.html`:

**Поле "Server address":**
- ❌ `localhost:8081` (это только локально)
- ✅ `your-server-url.replit.dev` (если используете Replit)
- ✅ `your-server-url.railway.app` (если используете Railway)
- ✅ Без `https://` - только domainимя:порт

**Галочка "Use Secure Connection":**
- ✓ Оставьте отмеченной

---

## 🎮 Готово! Теперь вы можете:

✅ Играть с другом из любой страны  
✅ Строить блоки вместе в реальном времени  
✅ Видеть позицию друга  
✅ Список кто онлайн  

**Управление:**
- Стрелки: движение
- Пробел: прыжок  
- Левый клик: поместить блок
- Правый клик: удалить блок

Приятной игры! 🚀
