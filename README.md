# Block Builder - Multiplayer Game

Онлайн мультиплеер игра для строительства блоков с поддержкой множества игроков.

## Возможности

✅ **Многопользовательская игра** - Играйте одновременно с друзьями  
✅ **Синхронизация в реальном времени** - WebSocket соединение для instant обновлений  
✅ **Совместное строительство** - Все игроки видят блоки друг другу  
✅ **Список активных игроков** - Видите кто онлайн и его статус  
✅ **Разные цвета игроков** - Каждый игрок имеет уникальный цвет  

## Установка

### ⚡ Быстрый старт (Python - рекомендуется)

**Если у вас установлен Python:**

1. Двойной клик на `run_python.bat` (Windows)
   или запустите в терминале:
   ```bash
   python server.py
   ```

2. Откройте `index.html` в браузере

3. Введите ваше имя и нажмите "Join Game"

---

### Вариант 1: Node.js

**Требования:**
- Node.js (v12.0 или выше)
- npm

**Шаги:**

1. Скачайте Node.js с https://nodejs.org/
   - **Важно:** При установке отметьте "Add to PATH"
2. Перезагрузитесь
3. Двойной клик на `run.bat` (Windows)
   или в терминале:
   ```bash
   npm install
   npm start
   ```

---

### Вариант 2: Python (альтернатива)

**Требования:**
- Python 3.6+

**Шаги:**

1. Скачайте Python с https://www.python.org/
   - **Важно:** При установке отметьте "Add Python to PATH"
2. Перезагрузитесь
3. Двойной клик на `run_python.bat` (Windows)
   или запустите:
   ```bash
   pip install websockets
   python server.py
   ```

---

### Запуск игры

1. **Запустите сервер** (выберите один из вариантов выше)

   Вы должны увидеть:
   ```
   ✓ Server running on ws://localhost:8080
   Waiting for players...
   ```

2. **Откройте игру в браузере**

   Дважды кликните на `index.html` или откройте его как ссылку:
   - Введите ваше имя
   - Нажмите "Join Game"

3. **Пригласите других игроков**

   Откройте `index.html` еще раз (в другом браузере, вкладке или другом компьютере):
   - Введите имя другого игрока
   - Нажмите "Join Game"

## Управление

**Клавиши управления:**
- `←` `→` - Движение влево/вправо
- `Space` или `↑` - Прыгать
- **ЛКМ** - Поместить блок
- **ПКМ** - Удалить блок

**Выбор цвета:**
- Нажимайте на цветные кнопки слева для выбора цвета блока

## Архитектура

### Клиент (`index.html`)
- WebSocket клиент для соединения с сервером
- Canvas для визуализации игры
- Локальное состояние игрока и других игроков

### Сервер (`server.js`)
- WebSocket сервер (ws://localhost:8080)
- Управление игровой сеткой (30x20)
- Синхронизация позиций игроков
- Трансляция действий (размещение/удаление блоков)

## Протокол WebSocket

### Сообщения от клиента к серверу:

**Join Game**
```json
{ "type": "join", "name": "PlayerName" }
```

**Move Player**
```json
{ "type": "movePlayer", "x": 100, "y": 50 }
```

**Place Block**
```json
{ "type": "placeBlock", "x": 5, "y": 10, "color": "#FF0000" }
```

**Delete Block**
```json
{ "type": "deleteBlock", "x": 5, "y": 10 }
```

### Сообщения от сервера к клиенту:

**Your ID**
```json
{ "type": "yourId", "id": 1 }
```

**Players List**
```json
{ "type": "playersList", "players": [...] }
```

**Player Joined**
```json
{ "type": "playerJoined", "playerId": 2, "name": "Player2", "x": 100, "y": 50, "color": "#FFD700" }
```

**Player Move**
```json
{ "type": "playerMove", "playerId": 2, "x": 120, "y": 60 }
```

**Block Placed**
```json
{ "type": "blockPlaced", "x": 5, "y": 10, "color": "#FF0000" }
```

## Сетевые параметры

- **Адрес сервера**: localhost:8080
- **Протокол**: WebSocket (ws://)
- **Максимум игроков**: Нет ограничений (зависит от сервера)
- **Интервал sync позиций**: 100ms
- **Размер сетки**: 30x20 блоков

## Известные ограничения

⚠️ Сервер не сохраняет состояние после перезагрузки  
⚠️ Нет аутентификации (в production нужна безопасность)  
⚠️ Нет ограничений на количество блоков  

## Возможные улучшения

- 🔧 Система сохранения миров
- 🔧 Аутентификация и аккаунты
- 🔧 Чат между игроками
- 🔧 Различные типы блоков
- 🔧 Баланс команд
- 🔧 Поддержка мобильных устройств
- 🔧 Серверная часть на cloud (AWS, Heroku)

## Лицензия

MIT

---

Создано для демонстрации WebSocket мультиплеера на Canvas.
